{
  "name": "wowcam-scraper",
  "version": "1.0.1",
  "main": "src/app.js",
  "scripts": {
    "dev": "node --watch src/app.js",
    "build": "esbuild src/app.js --bundle --platform=node --external:playwright --outfile=release/scrape.cjs",
    "prod": "node release/scrape.cjs"
  },
  "author": "MBODM (mbodm.com)",
  "license": "MIT",
  "description": "WOWCAM backend service to scrape addon data from the web (by using Playwright)",
  "type": "module",
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  }
}