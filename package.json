{
  "name": "wowcam-scraper",
  "version": "1.0.1",
  "main": "src/app.js",
  "scripts": {
    "dev": "node --watch src/app.js",
    "build": "esbuild src/app.js --format=esm --bundle --platform=node --external:playwright --outfile=release/scrape.mjs",
    "dcup": "sudo docker compose -f docker/docker-compose.yml up -d --build",
    "dcdown": "sudo docker compose -f docker/docker-compose.yml down",
    "dclogspw": "sudo docker compose -f docker/docker-compose.yml logs playwright",
    "dclogscaddy": "sudo docker compose -f docker/docker-compose.yml logs caddy"
  },
  "author": "MBODM (mbodm.com)",
  "license": "MIT",
  "description": "WOWCAM backend service to scrape addon data from the web (by using Playwright)",
  "type": "module",
  "dependencies": {
    "playwright": "^1.54.2"
  },
  "devDependencies": {
    "esbuild": "0.25.2"
  }
}